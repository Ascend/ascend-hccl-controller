---
- hosts:
    - harbor
    - master
  tasks:
  - name: set HARBOR_IP
    include_tasks: task_set_harbor_ip.yaml

# master install prom
- hosts: master
  gather_facts: False
  roles:
    - role: mindx.k8s.prom
