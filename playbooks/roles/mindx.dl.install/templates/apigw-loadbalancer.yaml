apiVersion: v1
kind: Service
metadata:
  name: apigw
  namespace: {{ K8S_NAMESPACE }}
spec:
  selector:
    app: apigw
  ports:
    - port: {{ APIGW_LOADBALANCER_PORT }}
      targetPort: {{ targetPort }}
  loadBalancerIP: {{ APIGW_LOADBALANCER_IP }}
  type: LoadBalancer
