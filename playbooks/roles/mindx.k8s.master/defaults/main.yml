# defaults file for mindx.k8s.master
calico_version: v3.20.2
kubernetes_version: v1.19.16
pod_network_cidr: "{{POD_NETWORK_CIDR}}"
k8s_namespace: "mindx-dl"
IMAGE_REPO: "{{HARBOR_IP}}:{{HARBOR_HTTPS_PORT}}"
K8S_REPO: "{{HARBOR_IP}}:{{HARBOR_HTTPS_PORT}}/k8s.gcr.io"
CALICO_YAML_DIR: "~/deploy_yamls/calico"
MASTER_IP: "{{ apiserver_advertise_address if apiserver_advertise_address is defined else ansible_default_ipv4.address}}"
